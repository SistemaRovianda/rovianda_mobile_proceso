export interface ProductCatalog {
  id: number;
  productName: string;
  code?: string;
  description?: string;
}

export interface ProductQualityCatalog{
  id:number,
  name:string,
  status:number
}
